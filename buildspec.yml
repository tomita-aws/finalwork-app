version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name finalwork-app --s3-location s3://finalwork-bucket/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src